# Snippet to fix 413 Request Entity Too Large for restaurant image uploads.
# Add this inside the server { } block that proxies to Django, or inside the
# location /api/ (or location /) that handles restrohubserver.sewabyapar.com.
#
# client_max_body_size 20M;
#
# Then run: sudo nginx -t && sudo systemctl reload nginx
#
# Example for a location block:
#
# location /api/ {
#     client_max_body_size 20M;
#     proxy_pass http://127.0.0.1:8000;   # or your Django upstream
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }
